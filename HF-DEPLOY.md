# Private Space deployment
Create the Space as PRIVATE before pushing `app.py`, `requirements.txt`, `tritowers_vision/`, `MODEL_CARD.md`, and `README-SPACE.md` as `README.md`. No Space is created by this branch.
